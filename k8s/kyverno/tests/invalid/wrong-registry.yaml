apiVersion: v1
kind: Pod
metadata:
  name: test-wrong-registry
  namespace: app-demo
spec:
  containers:
  - name: nginx
    image: nginx:latest  # VIOLATION: Not from Harbor registry
    resources:
      requests:
        cpu: "100m"
        memory: "128Mi"
      limits:
        cpu: "500m"
        memory: "512Mi"
