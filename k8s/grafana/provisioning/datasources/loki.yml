apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://host.docker.internal:31000
    isDefault: true
    editable: true
    jsonData:
      maxLines: 1000
      timeout: 60
